# ファシリテーション 学習地図 — A Learning Atlas

**「人と人とが関わるところに生じる問題を扱う営み」を、この地図ではファシリテーションと呼びます。**

会議の進行役という狭い意味ではありません。人が集まれば、話されていることの下で、
聴かれ方・関わり方・力関係といったものが同時に動きます。そのプロセスの側に働きかけて、
その場にいる人たちが自分たちで進んでいけるようにする営み——そう捉えると、
ワークショップも組織開発も対人支援も、同じ根から伸びた枝として見えてきます。

この地図は、それらの手法を**源流から遡ってたどれる**ようにしたものです。
手法を平置きの一覧にせず、どの研究伝統から生まれ、どの概念に属し、
何がその中核なのかが見えることを目指しています。

| 源流 | 中核概念 | 手法 | 人物 | 機関 | 書籍 |
|---:|---:|---:|---:|---:|---:|
| 13 | 38 | 501 | 105 | 9 | 56 |

---

## 4層の知識モデル

手法を平置きにせず、**重要度と包含関係**を持った階層でたどれるようにしています。

1. **源流（研究伝統）** — 13の研究伝統。最も広いストリーム。
2. **中核概念** — 38の概念（ラボラトリー方式の体験学習・学習する組織・U理論・成人発達論・システム思考など）。源流と手法のあいだの組織化レイヤ。
3. **手法** — 501の手法。各概念に**重要度 tier**（`1`=中核／`2`=主要／`3`=派生）で紐づきます。
4. **系譜・機関** — NTL・MIT Sloan・南山大学など。学派の系譜と、理論間・学者間の影響。

---

## 一緒に育ててください

**まちがいの指摘も、説明の書き直しも歓迎します。**
プログラミングの経験は要りません。ブラウザだけで提案できます。

**専用の編集画面があります**（`/admin`）。ファイルの書式も、YAML も、id も知らなくて構いません。
関連する手法や参考文献は、名前で検索して選ぶだけです。

→ **[CONTRIBUTING.md](CONTRIBUTING.md)** に、GitHub を使ったことがない方向けの手順があります。

いちばん簡単なのは、気づいたことを Issue に書くだけの方法です（3分）。
**「読んでも分からなかった」という指摘も、まちがいの指摘と同じくらい価値があります。**

あなたの編集がそのまま公開されることはありません。いったん確認を経てから反映されるので、
**壊す心配をせずに提案してください。**

---

## 現状の限界（先に知っておいてください）

この地図は**未完成の状態で公開しています**。完成を待つと、いつまでも開けないからです。
使う前に、いまどこが弱いのかを知っておいてください。

### 出典がほとんど付いていません

| | 出典あり |
|---|---|
| 手法 501件 | **7%**（36件） |
| うち中核手法 36件 | 61%（22件） |
| うち派生手法 274件 | **1%**（2件） |
| 人物 105件 | **2%**（2件） |

**記述の正しさを、この地図だけで確かめることはできません。** 重要な判断に使う前に、
必ず元の文献にあたってください。CC0 で「自由に使ってよい」と書いている以上、
この点は最初に伝えるべきだと考えています。

### 半分以上が監修前です

各レコードには `status` があり、`Draft` は**監修が済んでいない**ことを意味します。

- 手法：Draft 278（55%）／ Verified 222（44%）
- 人物：Draft 72（69%）／ Verified 33（31%）

### 分類は機械の推測を含みます

「どの手法がどの中核概念に属するか」は、人が指定した中核手法・主要手法を起点に、
関連の張られ方から機械が振り分けています。**派生手法（tier3）の分類は推測です。**
人が指定した分類は常に推測より優先されるので、おかしいと感じたら直せます。

### 書誌が食い違っている本が21件あります

同じ本が複数の手法に埋め込まれていた時代の名残で、著者名の表記や発行年が
枝分かれしたままのものが残っています。

---

**これらは欠陥であると同時に、貢献の入口でもあります。**
出典を1つ足す、Draft を1件確かめる、分類を1つ直す——どれも地図を確実に前へ進めます。
何から手を付けるか迷ったら、次を見てください。

```bash
python3 pipeline/audit_sources.py   # 出典が足りていない箇所（中核手法から順に）
python3 pipeline/report_gaps.py     # まだ書かれていない手法（参照された回数順）
```

---

## ライセンス

| | ライセンス |
|---|---|
| **データ**（`data/` と `dist/`） | [CC0 1.0](LICENSE) — パブリックドメイン。**誰でも、何にでも、断りなく使えます** |
| **コード**（`pipeline/`） | [MIT](LICENSE-CODE) |

この地図は既存の知恵を整理したものです。囲い込む理由がないので、手放しています。
クレジットは求めませんが、[CITATION.cff](CITATION.cff) を置いてあるので、
引用したい方はご利用ください。

> この判断は、地図が扱っている思想そのものから来ています。
> オードリー・タンとE・グレン・ワイルの『PLURALITY』——この地図にも収録しています——は
> CC0 で公開され、世界中の有志が多言語へ翻訳しました。
> 「対立を創造に変える協働」を紹介する地図が、閉じた編集体制であっては筋が通りません。

---

## リポジトリの構造

```
data/                 ← 正本。**直すのはここ**
├── methods/*.md         手法 501（YAML情報欄 + 本文）
├── people/*.md          人物 105
├── books/*.md           書籍 347
├── concepts/*.md        中核概念 38
├── disciplines/*.md     源流 13
├── institutions/*.md    機関 9
├── relations.yaml       人物どうしの影響（向き・種類・注釈つき）
├── concept-lineage.yaml 概念どうしの影響（A から B が生まれた）
├── roadmap.yaml         源流ごとの読書順路
├── aliases.yaml         手法の別名 → 正式な id
├── merges.yaml          統廃合された手法の転送先
└── maintenance/         運用メタデータ（表示には使わない）

admin/                ← 編集画面（Sveltia CMS）
├── index.html
└── config.yml           どの項目をどう編集させるかの定義

pipeline/             ← data/ から dist/atlas.json を作る
├── build.py             生成
├── validate.py          書き方の検査（日本語で指摘）
└── test_determinism.py  並び順に依存しないことの検査

dist/atlas.json       ← **生成物**。直しても次のビルドで消えます
```

### 「正本」と「生成物」を分けている理由

`dist/atlas.json` は `data/` から機械的に作られます。**生成物を直しても次のビルドで消えます。**

そして、正本には**人が判断したことだけ**を書きます。
「この手法がどの概念に属するか」「どの源流に連なるか」は、
関連の情報から機械が導き出すので、手で書く必要はありません。
手で書くと必ずずれていき、どちらが正しいのか誰にも分からなくなります。

ただし機械の推測はよく外れます。だから**人が上書きできる**ようにしてあります
（`concept:` を1行足すだけ。詳しくは CONTRIBUTING.md）。
機械は空欄を埋めるだけで、人の判断のほうが常に優先されます。

---

## ビルド

```bash
pip install pyyaml

python3 pipeline/validate.py         # 書き方の検査（日本語で指摘）
python3 pipeline/build.py            # dist/atlas.json を生成
python3 pipeline/test_integrity.py   # 関係が壊れていないかの検査
python3 pipeline/test_determinism.py # 並び順に依存しないことの検査
python3 pipeline/test_cms_config.py  # 編集画面が項目を落とさないかの検査
python3 pipeline/report_gaps.py      # まだ書かれていない手法の一覧
python3 pipeline/audit_sources.py    # 出典の充足状況
```

依存は PyYAML のみです。

---

## 公開されている地図

この地図は [helping](https://helping.jp) が運営し、
**https://helping.jp/facilitation-pedia** で公開しています。
helping は、ワークショップ・ファシリテーションの世界で培われてきた
「支援」「関わり」の力を、多くの人に開いていく試みです。
