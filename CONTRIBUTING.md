# この地図を一緒に育てる

この地図は、ファシリテーションの手法を源流からたどれるようにした知識の集まりです。
**まちがいの指摘も、説明の書き直しも、大歓迎です。**

このページは、**プログラミングの経験がない方**を想定して書いています。
GitHub を使ったことがなくても大丈夫です。順番に読めば、必ずたどり着けます。

---

## はじめに：壊す心配はありません

いちばん多いためらいが「自分がさわって壊してしまったらどうしよう」です。
その心配は要りません。**この仕組みでは、あなたの編集がそのまま公開されることはない**からです。

あなたが直したものは「**こう直したいです**」という提案として届きます。
中身を確認して、取り込むと決めたときに初めて地図に反映されます。
まちがえても、提案を取り下げれば何も起きません。取り込んだあとでも元に戻せます。

Wikipedia との違いはここです。Wikipedia は書いた瞬間に公開されますが、
この地図は**いったん誰かの目を通してから**公開されます。
だから、自信がなくても提案してもらって構いません。

---

## 4つの入口

やりたいことに合わせて選んでください。**下にいくほど手間がかかります。**
迷ったら A で構いません。A で伝えてもらえれば、誰かが B 以降をやります。

| | やること | 必要なもの | かかる時間 |
|---|---|---|---|
| **A. 気づいたことを伝える** | フォームに書くだけ | GitHub アカウント | 3分 |
| **B. 編集画面で直す** | 画面で書き換えて送る | GitHub アカウント | 10分 |
| C. ファイルを直接直す | ブラウザでファイルを編集 | GitHub アカウント | 15分 |
| D. まとめて直す | 手元にファイルを持ってきて直す | Git の知識 | — |

**ふだんは B を使ってください。** ファイルの書式を気にせず、
書籍や人物は一覧から選ぶだけで指定できます。`bk_0001` のような記号を見ることはありません。

---

## A. 気づいたことを伝える（いちばん簡単）

「ここの説明はまちがっている」「この手法が抜けている」「何を言っているのか分からない」——
**直し方が分からなくても、気づいたことだけ伝えてもらえれば十分です。**

1. リポジトリの上のほうにある **「Issues」** を押します。
   （Issue＝「課題」。この地図に対する気づきを書き残す掲示板だと思ってください）
2. **「New issue」** を押します。
3. 目的に合ったものを選びます。
   - **説明のまちがいを指摘する**
   - **手法・人物の追加を提案する**
   - **分類がおかしいと感じた**
   - **読んでも分からなかった**
4. フォームの質問に答えて、**「Submit new issue」** を押します。

これで終わりです。ファイルをさわる必要はありません。

> **「読んでも分からなかった」は、まちがいの指摘と同じくらい価値があります。**
> 分かりにくいのは読む人の問題ではなく、書き方の問題だからです。遠慮なく出してください。

---

## B. 編集画面で直す（おすすめ）

**専用の編集画面があります。** ファイルの書式を知らなくても編集できます。

### 手順

1. 地図のページから **「このページを直す」** を押すか、編集画面を直接開きます。
2. 初めてのときは **GitHub でログイン**します（アカウントがなければ2分で作れます）。
3. 左のメニューから種類を選びます。**手法・人物・書籍・中核概念・機関**。
4. 直したいものを探して開き、書き換えます。
5. **「保存」**を押すと、**提案として送られます**。

### 画面でできること

- **本文の書き直し** — ふつうの文章として書けます。太字や見出しはボタンで付けられます
- **関連する手法を足す** — 一覧から名前で探して選ぶだけ。**片側だけ選べば十分**です（相手側からのリンクは自動で張られます）
- **参考文献を足す** — 書名で検索して選びます。「この手法にとってなぜこの本か」も書けます
- **分類を直す** — 「中核概念」の欄で正しい概念を選びます

### 覚えなくてよいこと

- ファイル名、YAML の書き方、id の付け方
- 関連を両側に書くこと（機械が張ります）
- 源流の指定（中核概念が決まれば自動で決まります）

### 送ったあとに起きること

- 自動チェックが走ります（**数分**）。問題があれば**何がどう違うかを日本語で**教えてくれます。
  エラーが出ても失敗ではありません。
- 内容について質問や相談をすることがあります。そこでのやりとりも含めて、この地図の一部です。
- 取り込まれると、地図に反映されます。

---

## C. ファイルを直接直す

編集画面を使わず、ファイルそのものを直したい場合はこちらです。
（一括で直したいとき、編集画面がうまく動かないときなど）

1. 直したいファイルを開きます。手法なら `data/methods/`、人物なら `data/people/` の中です。
2. 右上の **鉛筆のアイコン（✏️）** を押します。
   - 初めてのときは「Fork this repository」と聞かれます。**押して先へ進んでください。**
     （Fork＝あなた専用の作業用コピー。元の地図はさわられないので安全です）
3. 文章を直します。
4. **「Commit changes...」** → **何を直したか**を一行で書く（日本語で構いません）
   → **「Propose changes」** → **「Create pull request」**

ファイルの読み方は次の節にあります。

## ファイルの読み方

手法や人物のファイルは、**上下2つの部分**でできています。

```markdown
---
id: t_group
ja: Tグループ
en: T-Group (Training Group)
status: Verified
people:
- klewin
desc: クルト・レヴィンらが1947年にNTLで始めた、ラボラトリー方式の体験学習の代表的手法。
---

## Tグループ

**概要**
Tグループは……（ここから下が本文）
```

- **上（`---` で挟まれた部分）＝ 情報欄**：名前・関連する人物・短い説明など、決まった項目を書きます。
- **下（2つめの `---` より下）＝ 本文**：解説の文章です。**ここは自由に書けます。**

### 本文を直すとき

いちばん気軽なのはここです。ふつうの文章として書き直してください。
`**太字**` のように書くと太字になり、`##` で始めると見出しになります。

### 情報欄を直すとき

**編集画面（B）を使えば、この形式を気にする必要はありません。**
ファイルを直接直す場合だけ、`項目: 値` の形を崩さないようにしてください。よくあるつまずきは3つです。

| つまずき | 例 | 直し方 |
|---|---|---|
| 行頭の空白がずれた | `  ja: Tグループ` | 行の先頭に余計な空白を入れない |
| 文の中に `:` がある | `ja: 対話: その本質` | 引用符で囲む → `ja: "対話: その本質"` |
| 一覧の書き方 | | 1行に1つ、行頭に `- ` を付ける |

自動チェックがこれらを見つけて日本語で教えてくれるので、覚えなくて大丈夫です。

---

## 出典のこと（この地図でいちばん大事なお願い）

**書いた内容の出どころを示してください。** 記憶や印象ではなく、確かめられる形で。

- 本・論文・公式サイトなど、**あとから誰かが確かめられるもの**を挙げてください。
- 日本語で読めるもの（邦訳・和書）を優先してください。多くの読者がたどれるからです。
- 情報欄の `sources` に足します。本文中に書いてもらっても構いません。

```yaml
sources:
- 津村俊充『プロセス・エデュケーション — 学びと変容のための対話』金子書房, 2012
```

### なぜここを厳しくするのか

この地図は **CC0**（誰でも自由に使ってよい）で公開しています。
つまり「使ってよい」と私たちが言った内容を、**知らない誰かがそのまま使います**。
まちがいがあれば、その人にまで迷惑が及びます。

そして出典は、読む人が**この地図を越えて先へ進むための入口**でもあります。
地図は目的地ではなく、通り道です。

---

## 分類について

各手法は「どの中核概念に属するか」で整理されています。この分類は、
**まず機械が関連手法から推測し**、そのあと人が直せるようになっています。

推測はよく外れます。「この手法がこの概念の下にあるのはおかしい」と感じたら、
それは**あなたの専門性がいちばん効く場所**です。

直し方は、**編集画面（B）でその手法を開き、「中核概念」の欄で正しい概念を選ぶだけ**です。
ファイルを直接直す場合は、情報欄に1行足します。

```yaml
concept: group_dynamics
```

人が指定した分類は、機械の推測より**必ず優先されます**。

---

## 書きぶりについて

- **評価や優劣を書かない。** 「最も優れた手法」ではなく、「何を扱う手法か」を書きます。
- **断定しすぎない。** 論争がある事柄は、争点があること自体を書きます。
- **専門用語には説明を添える。** 初めて出会う人が読むことを前提にします。
- 学術的な文体（である調）でも、話し言葉に近い文体（ですます調）でも構いません。
  ただし**1つのファイルの中では揃えて**ください。

---

## D. 手元で確認したい方へ

Git を使える方は、通常の fork → branch → PR で構いません。

```bash
pip install pyyaml
python3 pipeline/validate.py        # 書き方の検査（日本語で指摘します）
python3 pipeline/build.py           # dist/atlas.json を生成
python3 pipeline/test_determinism.py # 並び順に依存しないことの検査
```

`dist/atlas.json` は**生成物です**。ここを直しても次のビルドで消えるので、
必ず `data/` の側を直してください。

---

## 困ったら

分からなくなったら、途中でやめて Issue で聞いてください。
**「やり方が分からない」という Issue を立てて構いません。** 誰かが必ず答えます。

そもそも手順が分かりにくいのだとしたら、それはこの文書の問題です。
そのことも、ぜひ教えてください。
